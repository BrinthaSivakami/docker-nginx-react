#Travis config files

sudo: required
services:
  - docker

before_install:  
  - docker build -t brinthasivakami/reactdockertravis -f Dockerfile.dev .

script:
  - docker run -e CI=true brinthasivakami/reactdockertravi npm run test -- --coverage